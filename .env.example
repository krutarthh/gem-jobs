# Discord webhook URL for job alerts (create in Server Settings -> Integrations -> Webhooks)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# How often to scrape (minutes). Use 5 for "weapon" mode, 15-30 for MVP.
SCRAPE_INTERVAL_MINUTES=15

# Optional: path to watchlist YAML (default: config/watchlist.yaml)
# WATCHLIST_PATH=config/watchlist.yaml

# Optional: path to SQLite DB (default: data/jobs.db)
# DB_PATH=data/jobs.db
